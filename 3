#include<stdio.h> is header file which contain description of library function used in C program ,stdio.h stands for standard input output header file.

Printf is a keyword reading which the complier gives the output.
"main" is a user defined function,that is called by the operating system when the user runs the program.
Speed. Learning touch typing leads to a natural fluency when using a keyboard and faster typing speeds on average than someone using the 'hunt and peck' method. In the future this can lead to increased employability/job prospects but for right now it can mean less time on homework!
